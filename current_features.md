# OST Player - 現状機能・要素一覧 (Current Features)

OST Player (v3.4.1) に現在実装されている全機能および画面構成要素の一覧です。

---

## 1. プレイヤーモード (5 + 1 モード)

| モード名 | 機能概要 |
| :--- | :--- |
| **Single Mode** | 標準的な1曲再生プレイヤー。ターンテーブルジャケットアニメーション、アルバムアート表示、シークバー、音量、ループ切替 (Off/All/1/Game)、シャッフル、オートクロスフェード設定。ゲームサントラ特化 0ms シームレスループ対応。動画再生時は背景動画と完全同期。五度圏コードホイール表示対応。 |
| **Dual Mode** | 2つのデッキ（Deck A / Deck B）でDJミックスが可能。クロスフェードスライダー、同時再生（Play Both）、個別音量・シーク制御。 |
| **Multi Mode** | デッキを自由に追加（Add Deck）して複数曲を同時重ね再生。一括再生（Play All）・一括停止（Stop All）。 |
| **FX / Studio Mode** | エフェクト＆スタジオイコライザーモード。10バンドスタジオEQ、ボーカルキャンセラー、ステレオパン、Swap L/R、Mono Mix、Bitcrusher、テープディレイ、雨音・レコード環境音、ピッチ/速度、空間リバーブ、3D立体音響。WAVファイルへの書き出し保存対応。 |
| **Visual Mode** | フル画面ビジュアライザー（Quantum Orb, Neon Spectrum, Laser Wave, Radial Vortex, Starfield 3D Galaxy）による大画面音楽鑑賞。 |
| **Help View** | 操作ガイド、更新履歴 (Changelog)、ショートカット一覧、および **SSP (伺か) SSTP 連携設定**、**ソフトウェア更新 (Update Checker)** の統合画面。 |

---

## 2. 動画インポート & 背景動画同期再生 (Video Playback)

* **幅広い動画フォーマット対応**:
  * `.mp4`, `.webm`, `.mov`, `.mkv`, `.m4v`, `.avi`, `.ts`, `.ogv` などの主要な動画ファイルを通常楽曲と同様にインポート可能。
  * ファイル選択、フォルダ一括追加 (`webkitdirectory`)、ドラッグ＆ドロップで自動検知・読み込み。
  * IndexedDB (`tracks` ストア) に動画 Blob を安全に保存し、再起動後もライブラリを完全維持。
* **音ズレゼロ (0ms レイテンシ・ハードウェア同期)**:
  * 単一の `<video id="custom-bg-video">` 要素（`players[0]`）が映像描画と音声出力を同時に処理。
  * Web Audio API の `createMediaElementSource` に直結しているため、音声と映像が100%完全に一致し、シークや再生速度変更（0.5x〜2.0x）もズレなく追従。
  * 既存の EQ (イコライザー)、Bass Boost、リバーブ、3D立体音響、音量ノーマライザー (NORM)、ビジュアライザーが動画音声にもそのまま適用。
* **背景エフェクト連動 (Blur / Overlay / Card Opacity)**:
  * 背景動画再生時も **ぼかし (Blur)**、**暗さ (Overlay opacity)**、**パネル透明度 (Card opacity)** の調整スライダーがリアルタイムに適用。
* **背景連動トグル**:
  * テーマモーダル内に「動画再生時は背景を動画にする (Video Background)」設定を搭載（ON/OFF 切替、`localStorage` 永続保存）。
* **プレイリスト専用バッジ & 絞り込み**:
  * 動画トラックに `VIDEO` バッジを表示。
  * フィルターバーに `Video` ボタンを追加し、動画トラックのみのワンタップ絞り込みに対応。

---

## 3. SSP (伺か) SSTP 楽曲再生連携 (DirectSSTP 1.1)

* **ゴースト自動発話連携**: 音楽が再生されると、起動中の SSP (伺か) ゴーストへ曲名・アーティスト名・アルバム名を即時送信し、リアルタイムで喋らせる機能。
* **MP3/Audio ID3 タグ優先連携**: MP3 などの ID3 タグ (Title, Artist, Album) を自動解析し、タグに曲名が存在する場合はタグ名を優先してゴーストに送信。タグが存在しない場合はファイル名（拡張子なし）にシームレスにフォールバック。ID3v1 / ID3v2 (Shift_JIS / UTF-8) の文字化け防止に対応。
* **SakuraScript テンプレート編集**: `{title}` (曲名・タグ優先), `{artist}` (歌手), `{album}` (アルバム), `{filename}` (ファイル名) などの変数タグを含むスクリプトを自由にカスタマイズ可能。（デフォルト: `\0\s[0]『{title}』({artist})を再生中だよ！\e`）
* **アーティスト名スマートフォーマット**: アーティスト情報が未設定の楽曲では、`({artist})` や `（{artist}）` などの括弧部分が自然に自動除去されて発話されます。
* **SSTP ポート設定**: デフォルトの 9801 ポートから任意ポートへの変更に対応。
* **Help 画面での一括管理**: Help タブ上部に専用設定カードを配置（ON/OFF トグル、スクリプト入力、ポート設定、テスト発話ボタン）。設定内容は `localStorage` に即時自動保存。

---

## 4. MIDI & SoundFont サポート

* **デュアルMIDIエンジン**:
  * **WebAudioTinySynth**: 軽量即時再生エンジン
  * **SpessaSynth**: 高音質 SoundFont 対応 WebWorklet エンジン
* **SoundFont (SF2 / SF3) 対応**: カスタム SoundFont ファイルの読み込み・即時切り替え（Hot-swapping、再起動不要）・適用・IndexedDBへの永続保存・デフォルトリセット。
* **MIDI チャンネルミキサー (16CH)**:
  * チャンネル別のミュート（Mute）/ ソロ（Solo）/ 音量（Vol）/ 楽器（Instrument）選択
  * 音符発音時にリアルタイム点灯する LED インジケーター (Ch 1〜16)
  * **トラック個別設定の完全永続化 (Save & Persistence)**: チャンネル音量・ミュート・ソロ・カスタム音色（プログラム）を楽曲ごとに localStorage + IndexedDB に自動保存・自動復元。「設定を保存」ボタンと「保存設定」バッジを搭載。
  * **即時音色反映 (Real-time Instant Instrument Switching)**: プリセット選択時および各チャンネルの楽器変更時に、再生中の旧音色ボイスを即座に停止し、新音色（Program Change）をリアルタイムに合成エンジン（SpessaSynth / WebAudioTinySynth）へ即時反映。
  * **ワンクリックミキサープリセット**: 「Piano Focus」「Acoustic Ensemble」「80s Synthwave」「Orchestral」「8-Bit Chiptune」「全チャンネル解除」「デフォルト」等の音響バランスプリセットを選択可能。
  * **ミキサー初期化**: 「リセット」ボタンでワンクリック初期化。
  * **音色変更の永続維持 (Preset Lock)**: ミキサーのドロップダウンで選択した楽器が、MIDI 内部の Program Change イベントで元に戻らないよう presetLock 機構で保護。
  * **完全消音ガード**: ミュート中や非ソロ状態のチャンネルに対し、Note-On イベントを遮断し、CC7 (Volume) / CC11 (Expression) の音量指定を 0 固定・コントローラーロックして貫通を完全防止。
  * **シーク時状態復元**: シークバー移動時にもソロ/ミュート/音量/カスタム楽器設定が解除されず即時再適用。
  * **トラック切り替え時 0:00 リセット**: MIDI 曲間の切り替え時に再生時間とシークバーを確実に先頭（0:00）へ初期化。
* **MIDI ピアノロール (Piano Roll Visualizer)**:
  * 16チャンネルの音符をネオンカラーでリアルタイム描画する高速ウォーターフォール（1.1秒）
  * ピアノ風のクッキリとしたシャープ矩形デザイン
  * プレイヘッド（現在時間線）と発声中ノートのグロー発光エフェクト
* **MIDI -> WAV Audio 変換書き出し**: 適用中の SoundFont（SGM等）で再生中の MIDI トラックを、ワンクリックで高音質 `.wav` ファイルへエクスポート書き出し保存。
* **シームレス一時停止 (Pause / Resume)**: 演奏位置を維持したまま一時停止・再開をスムーズに実行。
* **SoundFont 常時表示バッジ**: 現在適用中の SoundFont (SGM / Built-in) をヘッダーに可視化表示。選択・リセット対応。
* **静的 MIDI 演奏時間計算 (Tempo Mapping)**: 途中でBPMが変動するMIDIファイルでも演奏時間・経過時間がズレない正確なタイム計算。

---

## 5. 音声エフェクト & スタジオ音響 (FX / Studio モード機能)

* **本格 10バンド・グラフィックイコライザー (10-Band Studio EQ)**:
  * 31Hz, 62Hz, 125Hz, 250Hz, 500Hz, 1kHz, 2kHz, 4kHz, 8kHz, 16kHz の利得調整（Presetプリセット対応）。
  * 5-Band (Simple) ↔ 10-Band (Studio) ワンタップ切り替えトグル。
* **センターボーカルキャンセラー / カラオケモード (Vocal Cut)**:
  * ステレオ音源の中央成分を逆位相合成で消音し、即座にインスト化（0%〜100%カット強度スライダー）。
* **左右独立パン & チャンネルルーティング**:
  * 左右定位（Stereo Pan）スライダー（ダブルクリックでCenter復帰）。
  * Swap L/R (左右チャンネル反転)、Mono Mix (モノラル合成)。
* **Bass Boost**: 重低音エンハンスメント
* **Reverb**: 空間残響エフェクト
* **Distortion**: ディストーション（歪み）エフェクト
* **Echo / Delay**: ディレイエフェクト
* **Tremolo**: 音量振動トレモロエフェクト
* **Speed & Pitch Lock (Time Stretch)**: 0.5x 〜 2.0x の速度変更（ピッチ維持/ロック機能付き）
* **Lowpass / Highpass Filter**: 周波数カットオフフィルター
* **3D Spatial Audio & 8D Auto-Orbit**:
  * Web Audio HRTF 3D PannerNode による立体音響
  * 音源が頭の周囲を自動旋回する **8D Auto-Orbit モード**
  * ドラッグで音源位置（前後左右）を自在に変更できる **2D Panner パッド**
* **WAV エクスポート**: エフェクトをかけた状態の音声を WAV ファイルとして書き出しダウンロード

---

## 6. メタデータ & アルバムアート & ゲームループ解析

* **ID3v2 & APIC タグ解析**: MP3/Audio ファイルの曲名・アーティスト名・アルバム名・**埋め込みカバーアート画像（Cover Art）**を非同期自動抽出。
* **ゲームサントラ特化 0ms シームレスループ (Game OST Loop)**: Ogg Vorbis, FLAC, MP3, WAV に埋め込まれた `LOOPSTART`, `LOOPLENGTH`, `LOOPEND` タグを自動検出して 0ms シームレスループ再生。
* **五度圏コードホイール (Circle of Fifths)**: MIDI 再生中にリアルタイムで和音・ポリゴンを描画。
* **ターンテーブル中央 ＆ サムネイル表示**: 抽出したアルバムアートをSingle Modeの中央ジャケットおよびプレイリスト各項目に表示。

---

## 7. 便利な再生・UI機能

* **ミニプレイヤーモード (Mini Player Mode)**: 1行に収まるコンパクト表示切替（常に最前面表示・プレイリスト連動）
* **セッション録音 (Rec Session)**: マスター音声をリアルタイム録音（WebM生成、プレビュー再生ダイアログ、WAVエクスポート対応）
* **オートクロスフェード (Auto Crossfade)**: 曲の終わりに指定秒数（1〜12秒）で自動クロスフェード切替

---

## 8. ライブラリ & プレイリスト管理

* **IndexedDB 永続ストレージ**: 追加した音楽・動画ファイル、メタデータ、カスタム背景画像をブラウザ内に自動保存。再読み込み後もライブラリが維持されます。
* **プレイリスト編集強化**:
  * **ドラッグ＆ドロップ並び替え**: トラックを直接ドラッグして再生順序を自在に変更。
  * **プレイリスト名変更 (Rename)**: プレイリスト名を簡単変更。
  * **プレイリスト複製 (Duplicate)**: プレイリストをワンタップでコピー複製。
* **トラック検索 & フィルター**:
  * トラック名・アーティスト名のリアルタイムあいまい検索
  * **`All`** / **`MIDI`** / **`Audio`** / **`Video`** の形式別ワンタップ絞り込みフィルター
* **JSON バックアップ (Import / Export)**: プレイリスト構成の JSON エクスポート / インポート（実ファイル完全復元対応）。
* **トラック個別削除 & 全消去**: 確認モーダルダイアログ付きの安全なトラック削除。
* **ファイル・フォルダ追加**: ファイル選択、フォルダ一括追加 (`webkitdirectory`)、ドラッグ＆ドロップ用オーバーレイ。

---

## 9. テーマ & デザインカスタマイズ

* **8つのプリセットテーマ**:
  1. **Cyberpunk**
  2. **Synthwave**
  3. **Emerald**
  4. **Studio Light**
  5. **Game Boy**
  6. **Clear Glass**
  7. **Custom Image**
  8. **Video** (半透明グラスモーフィズム & ネオンローズ)
* **カスタム背景画像設定**:
  * 背景画像のドラッグ＆ドロップ設定
  * ぼかし (Blur) 調整スライダー (0〜25px)
  * 暗さ (Overlay opacity) 調整スライダー
  * パネル透明度 (Card opacity) 調整スライダー
  * 画像クリア機能・IndexedDB保存
* **背景アンビエントビジュアライザー (Ambient Visualizer)**:
  * `Spectrum` (周波数スペクトラム)
  * `Fluid Wave` (流体ウェーブ)
  * `Particles` (浮遊粒子)
  * `Beat Bounce` (ビート跳ね)

---

## 10. キーボードショートカット

* `Space`: 再生 / 一時停止 (全モード共通)
* `F11`: フルスクリーンの切り替え
* `1` 〜 `6`: プレイヤーモード切替 (Single, Dual, Multi, FX, Visual, Help)
* `←` / `→`: 前の曲 / 次の曲 (Single, Visual, FX)
* `↑` / `↓`: 音量の上下
* `[` / `]`: Dualモードのクロスフェーダー調整
* `M`: 全体ミュートの切り替え

---

## 11. 隠し実績システム (Achievements) & イースターエッグ (v3.2.0)

* **実績 (Achievements)**:
  * 全 40 種類の隠し実績を搭載（v3.2.0にてNo. 32〜No. 40を追加）。各実績には解除条件、説明、ヒントがある
  * カテゴリ: 再生・リスニング (9種) / MIDI・音源 (9種) / 音響FX・3D (7種) / DJ・機能 (5種) / ビジュアル・テーマ (6種) / シークレット (4種)
  * 解除時: 右上にスライドインするトースト通知 + 短い解除音を再生
  * 進捗・状態は localStorage + IndexedDB に永続保存
* **トロフィールーム**:
  * ヘッダーのトロフィーボタンからモーダルを開き、全実績の一覧・進捗率・プレイヤー称号を確認
  * カテゴリタブによるフィルタリング対応
  * 未解除実績はロックアイコン + ヒント表示のみ、解除済みはゴールドバッジ + 解除日時
  * プレイヤー称号システム: 解禁数に応じて6段階で称号が変化
  * 実績データ初期化ボタン付き
* **イースターエッグ「Oia Uia!」 & コナミコマンド**:
  * 特定の条件やコナミコマンド（↑↑↓↓←→←→BA）で踊るスピニングキャットが画面右下に出現
  * 出現するとクリック操作が可能
  * 出現時・コマンド入力時に対応する実績を解除

---

## 12. ソフトウェア更新 (Software Updates / In-App One-Click Auto Updater) (v3.4.1)

* **GitHub Releases API 連携**:
  * `https://api.github.com/repos/{owner}/{repo}/releases/latest` から最新バージョン情報とリリースノートを取得。
  * セマンティックバージョニング比較 (`compareVersions`) により、現在のアプリバージョン (`v3.4.1`) と最新タグを自動判定。
* **ワンクリック・インプレイス自動アップデート (One-Click In-App Auto Updater)**:
  * ユーザーによる ZIP 解凍や手動ファイル配置が一切不要な、完全自動インプレイス更新エンジンを搭載。
  * 更新通知モーダルで「今すぐワンクリック更新 (自動再起動)」を押すだけで、最新リリースパッケージの自動ダウンロード、アーカイブ展開、ファイルの自動置換、およびアプリの自動再起動が 1 クリックで完結。
  * ダウンロード進捗（受信容量 / 総容量 / パーセンテージ）および展開・適用ステージをリアルタイム・プログレスバーで可視化。
  * 従来のブラウザ手動ダウンロードへのフォールバック機能も維持。
* **Help 画面での一括管理カード**:
  * 現在バージョンバッジ (`v3.4.1`) および更新ステータスバッジの表示。
  * GitHub リポジトリ指定フィールド（デフォルト: `yukkurikasutera3/ostplayer`、`localStorage` 永続保存、初期値リセットボタン付き）。
  * 起動時の自動更新確認トグル（ON/OFF、`localStorage` 永続保存）。
  * 「Releases ページを開く」ボタンおよび「更新を確認」ボタン（ローディング表示対応）。
* **アップデート通知モーダル (Update Modal)**:
  * 現在バージョンと最新バージョンの比較表示、公開日、リリースタイトル、ダウンロード対象ファイル名・容量を表示。
  * スクロール可能なリリースノート・Changelog 閲覧ボックス。
  * 「今すぐワンクリック更新 (自動再起動)」ボタンと「ブラウザで手動保存」ボタンを搭載。
* **起動時バックグラウンド自動確認**:
  * アプリ起動後 3.5 秒後にバックグラウンドで自動確認を実行（起動時のUI描画を妨げない非同期処理）。

